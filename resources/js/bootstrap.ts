/**
 * Nodal — Bootstrap
 * ==================
 * Configurações iniciais da aplicação frontend.
 * Este arquivo é carregado antes de qualquer componente React.
 */

// Axios não é mais necessário com Inertia v3 (usa XHR nativo).
// Este arquivo existe como ponto de extensão para futuras configurações globais.
